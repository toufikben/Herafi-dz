# Supabase setup for Herafi DZ

## الهدف

يستخدم المشروع Supabase كطبقة Backend مركزية مستقبلية، مع إبقاء Room داخل تطبيق Android كذاكرة محلية مؤقتة أثناء مرحلة الانتقال. لا تستبدل هذه المرحلة قاعدة Room تلقائيًا؛ فهي تجهز المخطط وسياسات الوصول أولًا حتى يمكن اختبار الربط بأمان.

## إنشاء المشروع

أنشئ مشروعًا في [Supabase Dashboard](https://supabase.com/dashboard)، ثم افتح SQL Editor والصق محتوى `supabase/schema.sql`. راجع سياسات Row Level Security قبل نشر بيانات حقيقية.

في إعدادات المشروع، انسخ Project URL والمفتاح العام `anon` فقط. ضع القيم في ملف `.env` المحلي:

```text
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
```

لا تضع `service_role` أو أي مفتاح إداري داخل تطبيق Android أو في Git. ملف `.env` مستبعد من Git، بينما `.env.example` يحتوي على placeholders فقط.

## بنية البيانات الأولية

يحتوي المخطط على `profiles` للمستخدمين، و`craftsmen` للحرفيين، و`reviews` للتقييمات، و`bookmarks` للمفضلة، و`service_requests` للطلبات المستقبلية. يبدأ الحرفي بالحالة `pending` ولا يصبح `published` إلا بعد مراجعة الإدارة.

## خطة الربط التدريجي

في الخطوة التالية تضاف طبقة Repository تعتمد على Retrofit أو عميل Supabase، وتقرأ الحرفيين المنشورين من API ثم تحفظ نسخة Room محليًا. إذا انقطع الاتصال، يستخدم التطبيق آخر نسخة محلية. الكتابة الحساسة، مثل اعتماد الحرفي أو تعديل حالته، يجب أن تمر عبر سياسات RLS أو Edge Function، ولا يجوز تنفيذها بالمفتاح الإداري من الهاتف.

## التحقق قبل الإنتاج

يجب اختبار القراءة كمستخدم مجهول، وإنشاء ملف كمستخدم مصادق، منع مستخدم من تعديل ملف مستخدم آخر، منع قراءة ملفات الحرفيين غير المنشورة، ومنع إضافة تقييم باسم مستخدم آخر. يجب أيضًا تفعيل النسخ الاحتياطية ومراقبة الاستهلاك قبل استقبال بيانات حقيقية.

## الحالة الحالية

هذه المرحلة تضيف المخطط والتوثيق وإعدادات البيئة فقط. لم تُربط واجهة التطبيق بعد بالـAPI حتى لا تتوقف النسخة المحلية قبل تهيئة مشروع Supabase فعليًا ووضع قيم البيئة الصحيحة.
